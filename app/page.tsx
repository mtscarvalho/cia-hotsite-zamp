export default function Home() {
  return (
    <main>
      <section className="hero">
        <p className="eyebrow">Next.js static export</p>
        <h1>CIA Hotsite Zamp</h1>
        <p className="lead">
          The new project is ready. Running <code>npm run build</code> generates a
          fully static site in <code>dist/</code>, ready for the existing publisher.
        </p>
      </section>
    </main>
  );
}
